# Copy this file to .env.local and adjust as needed. .env.local is gitignored.
# Must match the backend's DEMO_*_PASSWORD values (apps/backend/.env) if you
# change them there — these only pre-fill the login screen's demo accounts.

NEXT_PUBLIC_DEMO_ADMIN_PASSWORD=
NEXT_PUBLIC_DEMO_MANAGER_PASSWORD=
NEXT_PUBLIC_DEMO_VIEWER_PASSWORD=
